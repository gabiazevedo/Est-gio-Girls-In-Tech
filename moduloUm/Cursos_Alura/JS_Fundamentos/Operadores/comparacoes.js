// == (comparação implícita)

const numero = 5;
const texto = '5';

console.log(numero === texto);

//typeof

consolelog(typeof numero);
consolelog(typeof texto);

// == só compara o valor
// === compara o valor e o tipo de dado

// conversão explícita

Number()
String()
